{
    "extends": "@local/configs/typescript.json",
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "$/*": ["*"]
        }
    },
    "exclude": ["node_modules", "apps", "packages"]
}
