dist: focal

language: node_js

node_js:
    - 20.10.0

cache:
    npm: false
    directories:
        - ~/.pnpm-store

before_install:
    - npm add -g pnpm@8.10.5
    - pnpm config set store-dir ~/.pnpm-store

install:
    - pnpm i
    - pnpm build

script:
    - pnpm run ci
